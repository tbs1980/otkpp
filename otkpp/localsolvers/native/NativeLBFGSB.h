
// TODO: work in progress...

#ifndef NATIVELBFGSB_H

/*#include "GradientSolver.h"

class NativeLBFGSB : public GradientSolver
{
 public:
 private:
  vector< double > l_, u_;
  int m_; 
  
  void computeGenCP_(vector< double > &x_cp);
  IterationStatus iterate_();
  void setup_(const Function &objFunc,
              const vector< double > &x0,
              const SolverSetup &solverSetup,
              const Constraints &C);
};*/

#define NATIVELBFGSB_H

#endif
